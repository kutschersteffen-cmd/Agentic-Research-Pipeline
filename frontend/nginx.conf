server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: any path not matching a real static file resolves
        # to index.html so client-side routing (if/when added) still works.
        try_files $uri /index.html;
    }
}
