# @gorules/zen-engine-wasm32-wasi (the rule engine the Decision Studio runs in
# the browser) declares cpu "wasm32", so npm refuses it on x64/arm64 hosts even
# though it is only ever executed by the browser. force skips that platform check;
# every other dependency still resolves from package-lock.json as usual.
force=true
